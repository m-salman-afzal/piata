{
    "configurations": [
        {
            "name": "Launch Edge",
            "request": "launch",
            "type": "msedge",
            "url": "https://127.0.0.1:3001",
            "webRoot": "${workspaceFolder}"
        }
    ]
}